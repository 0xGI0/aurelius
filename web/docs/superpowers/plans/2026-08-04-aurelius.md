# Aurelius Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mehrsprachige Marc-Aurel-Zitate-App (Expo/React Native): Tap → Abschnitt aus dem kompletten Werk in DE/EN/Altgriechisch, optional KI-Erklärung als Live-Stream (BYOK Claude, Fallback Gemini via Vercel-Function).

**Architecture:** Expo-App (TypeScript, Expo Router, 2 Screens) mit gebündeltem `data/quotes.json` (alle ~490 Abschnitte, 3 Sprachen — per Node-Skripten aus gemeinfreien Quellen gebaut). KI-Schicht als Provider-Interface `AsyncIterable<string>`; Anthropic direkt vom Gerät (Streaming), Gemini über `/api/explain` (Vercel Node Function). Design-Tokens in `theme/`, zusätzlich HTML-Referenzkarten für claude.ai/design.

**Tech Stack:** Expo SDK (aktuell), TypeScript strict, Expo Router, Jest (`jest-expo`), `tsx` für Skripte, `@anthropic-ai/sdk`, `@google/genai` (nur Serverfunktion), `expo-secure-store`, `@expo-google-fonts/cormorant-garamond` + `@expo-google-fonts/gfs-didot`, Vercel (Web + Function), EAS Build (Android-APK).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-08-04-aurelius-design.md` — bei Widerspruch gewinnt die Spec.
- UI-Sprache: **Deutsch**. Zitat-Sprachen: `de` | `en` | `grc`.
- Anthropic-Modell: **`claude-opus-5`** (exakt dieser String). Gemini-Modell: `gemini-2.5-flash` (bei Task 10 gegen aktuelle Docs verifizieren).
- Quellen ausschließlich gemeinfrei: Wittstock-PDF (`data-sources/Selbstbetrachtungen-Wittstock-Reclam.pdf`), Long 1862 (Project Gutenberg #2680), griechischer Text (PerseusDL, Teubner 1908). **Keine modernen Übersetzungen.**
- Umgebung: `npm`/`npx` und `git commit` benötigen Sandbox-Bypass (bekannt: npm-Cache EROFS; GPG-Signierung braucht gpg-agent).
- Nach jedem Task: alle Tests grün, dann Commit (Conventional Commits, deutsch).
- TypeScript `strict: true`; keine zusätzlichen Abhängigkeiten über die hier genannten hinaus.

---

### Task 1: Expo-Scaffold + Jest

**Files:**
- Create: komplettes Expo-Projekt im Repo-Root (`package.json`, `app/`, `tsconfig.json`, `app.json`, `.gitignore`)
- Create: `jest.config.js`

**Interfaces:**
- Produces: lauffähiges Expo-Projekt mit Expo Router; `npm test` läuft (0 Tests ok); Ordner `app/` mit `_layout.tsx`, `index.tsx`.

- [ ] **Step 1: Expo-App in Temp-Ordner erzeugen und in den Repo-Root mergen** (create-expo-app verweigert nicht-leere Ordner)

```bash
cd /home/x/Dokumente/Github/aurelius
npx create-expo-app@latest tmp-scaffold --template default
rsync -a --exclude .git tmp-scaffold/ ./
rm -rf tmp-scaffold
```

- [ ] **Step 2: Beispiel-Screens des Templates entfernen, Minimal-Router aufsetzen**

`app/_layout.tsx`:
```tsx
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack screenOptions={{ headerShown: false }}>
      <Stack.Screen name="index" />
      <Stack.Screen name="settings" options={{ presentation: 'modal' }} />
    </Stack>
  );
}
```

`app/index.tsx` (Platzhalter, wird in Task 11 ersetzt):
```tsx
import { Text, View } from 'react-native';

export default function Home() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Aurelius</Text>
    </View>
  );
}
```

`app/settings.tsx` (Platzhalter, wird in Task 12 ersetzt):
```tsx
import { Text, View } from 'react-native';

export default function Settings() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Einstellungen</Text>
    </View>
  );
}
```

Alle übrigen vom Template angelegten Screens/Komponenten (`app/(tabs)/`, Beispiel-`components/`, `constants/`, Beispiel-Assets außer Icon/Splash) löschen. In `app.json` setzen: `"name": "Aurelius"`, `"slug": "aurelius"`, `"scheme": "aurelius"`.

- [ ] **Step 3: Jest + tsx installieren und konfigurieren**

```bash
npx expo install jest-expo jest
npm i -D tsx @types/jest
```

`jest.config.js`:
```js
module.exports = {
  preset: 'jest-expo',
  testMatch: ['**/__tests__/**/*.test.ts', '**/__tests__/**/*.test.tsx'],
};
```

`package.json` scripts ergänzen: `"test": "jest"`.

- [ ] **Step 4: Verifizieren**

Run: `npm test -- --passWithNoTests` → Expected: exit 0.
Run: `npx tsc --noEmit` → Expected: keine Fehler.
Run: `npx expo export -p web` → Expected: `dist/` wird erzeugt (danach `rm -rf dist`, `dist` in `.gitignore`).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: Expo-Scaffold mit Router und Jest"
```

---

### Task 2: Datenmodell & ShuffleBag (TDD)

**Files:**
- Create: `lib/quotes.ts`
- Test: `lib/__tests__/quotes.test.ts`

**Interfaces:**
- Produces:
  - `type QuoteLang = 'de' | 'en' | 'grc'`
  - `interface Quote { id: string; book: number; section: number; texts: Record<QuoteLang, string> }`
  - `class ShuffleBag { constructor(ids: string[], rng?: () => number); next(): string }` — liefert jede ID genau einmal pro Runde, keine unmittelbare Wiederholung über Rundengrenzen.
  - `function formatReference(q: Pick<Quote, 'book' | 'section'>): string` → `"Buch IV, 7"` (römische Buchzahl).

- [ ] **Step 1: Failing Tests schreiben**

`lib/__tests__/quotes.test.ts`:
```ts
import { ShuffleBag, formatReference } from '../quotes';

function seededRng(seed: number): () => number {
  let s = seed;
  return () => {
    s = (s * 1664525 + 1013904223) % 4294967296;
    return s / 4294967296;
  };
}

describe('ShuffleBag', () => {
  it('liefert jede ID genau einmal pro Runde', () => {
    const ids = ['a', 'b', 'c', 'd'];
    const bag = new ShuffleBag(ids, seededRng(1));
    const round = [bag.next(), bag.next(), bag.next(), bag.next()];
    expect([...round].sort()).toEqual([...ids].sort());
  });

  it('wiederholt die letzte ID nicht direkt beim Rundenwechsel', () => {
    for (let seed = 1; seed <= 20; seed++) {
      const bag = new ShuffleBag(['a', 'b', 'c'], seededRng(seed));
      const round1 = [bag.next(), bag.next(), bag.next()];
      expect(bag.next()).not.toBe(round1[2]);
    }
  });

  it('funktioniert mit einer einzigen ID', () => {
    const bag = new ShuffleBag(['solo']);
    expect(bag.next()).toBe('solo');
    expect(bag.next()).toBe('solo');
  });
});

describe('formatReference', () => {
  it('formatiert mit römischer Buchzahl', () => {
    expect(formatReference({ book: 4, section: 7 })).toBe('Buch IV, 7');
    expect(formatReference({ book: 12, section: 36 })).toBe('Buch XII, 36');
  });
});
```

- [ ] **Step 2: Testlauf — muss fehlschlagen**

Run: `npm test -- quotes` → Expected: FAIL (`Cannot find module '../quotes'`).

- [ ] **Step 3: Implementierung**

`lib/quotes.ts`:
```ts
export type QuoteLang = 'de' | 'en' | 'grc';

export interface Quote {
  id: string;
  book: number;
  section: number;
  texts: Record<QuoteLang, string>;
}

const ROMAN = ['', 'I', 'II', 'III', 'IV', 'V', 'VI', 'VII', 'VIII', 'IX', 'X', 'XI', 'XII'];

export function formatReference(q: Pick<Quote, 'book' | 'section'>): string {
  return `Buch ${ROMAN[q.book]}, ${q.section}`;
}

export class ShuffleBag {
  private bag: string[] = [];
  private last: string | null = null;

  constructor(private ids: string[], private rng: () => number = Math.random) {}

  next(): string {
    if (this.bag.length === 0) this.refill();
    const id = this.bag.pop()!;
    this.last = id;
    return id;
  }

  private refill(): void {
    this.bag = [...this.ids];
    for (let i = this.bag.length - 1; i > 0; i--) {
      const j = Math.floor(this.rng() * (i + 1));
      [this.bag[i], this.bag[j]] = [this.bag[j], this.bag[i]];
    }
    // Keine unmittelbare Wiederholung über die Rundengrenze:
    const top = this.bag.length - 1;
    if (this.bag.length > 1 && this.bag[top] === this.last) {
      [this.bag[top], this.bag[0]] = [this.bag[0], this.bag[top]];
    }
  }
}
```

- [ ] **Step 4: Testlauf — muss bestehen**

Run: `npm test -- quotes` → Expected: PASS (4 Tests).

- [ ] **Step 5: Commit**

```bash
git add lib && git commit -m "feat: Quote-Datenmodell und ShuffleBag"
```

---

### Task 3: Parser Deutsch (Wittstock-PDF)

**Files:**
- Create: `scripts/parsers/de.ts`, `scripts/extract-de.ts`
- Test: `scripts/parsers/__tests__/de.test.ts`
- Output (generiert, committen): `data-sources/extracted/de.json`

**Interfaces:**
- Produces: `function parseDe(raw: string): Array<{ book: number; section: number; text: string }>`; Extraktionsskript schreibt `data-sources/extracted/de.json` (Array dieser Objekte).
- Format-Annahme (nach `pdftotext`-Extraktion): Bücher beginnen mit eigener Zeile `Erstes Buch` … `Zwölftes Buch` (auch `Siebentes` akzeptieren); Abschnitte beginnen am Zeilenanfang mit `N. ` (arabisch); Kopfzeilen `Marc Aurel – Selbstbetrachtungen …` und Seitenzahlen `| N |` müssen entfernt werden; alles vor dem ersten Buch (Titelei, Einleitung) wird verworfen.

- [ ] **Step 1: Failing Test mit Fixture schreiben**

`scripts/parsers/__tests__/de.test.ts`:
```ts
import { parseDe } from '../de';

const FIXTURE = `Marc Aurel – Selbstbetrachtungen                    Einleitung
Einleitung
Dies ist Vorgeplänkel und muss verworfen werden.
| 4 |
Erstes Buch
1. Von meinem Großvater Verus habe ich gelernt,
sanftmütig zu sein.
2. Von dem Rufe meines Erzeugers: Bescheidenheit
und männlichen Charakter.
| 9 |
Marc Aurel – Selbstbetrachtungen                    Erstes Buch
Zweites Buch
1. Des Morgens sage dir selbst: Heute werde ich
Zudringlichen begegnen.
`;

describe('parseDe', () => {
  it('verwirft Einleitung, erkennt Bücher und Abschnitte, entfernt Kopf-/Fußzeilen', () => {
    const result = parseDe(FIXTURE);
    expect(result).toEqual([
      { book: 1, section: 1, text: 'Von meinem Großvater Verus habe ich gelernt, sanftmütig zu sein.' },
      { book: 1, section: 2, text: 'Von dem Rufe meines Erzeugers: Bescheidenheit und männlichen Charakter.' },
      { book: 2, section: 1, text: 'Des Morgens sage dir selbst: Heute werde ich Zudringlichen begegnen.' },
    ]);
  });
});
```

- [ ] **Step 2: Testlauf — muss fehlschlagen**

Run: `npm test -- parsers/__tests__/de` → Expected: FAIL.

- [ ] **Step 3: Parser implementieren**

`scripts/parsers/de.ts`:
```ts
const BOOKS: Record<string, number> = {
  'Erstes Buch': 1, 'Zweites Buch': 2, 'Drittes Buch': 3, 'Viertes Buch': 4,
  'Fünftes Buch': 5, 'Sechstes Buch': 6, 'Siebentes Buch': 7, 'Siebtes Buch': 7,
  'Achtes Buch': 8, 'Neuntes Buch': 9, 'Zehntes Buch': 10, 'Elftes Buch': 11,
  'Zwölftes Buch': 12,
};

export interface ParsedSection {
  book: number;
  section: number;
  text: string;
}

export function parseDe(raw: string): ParsedSection[] {
  const lines = raw
    .split('\n')
    .map((l) => l.trim())
    .filter((l) => !/^Marc Aurel – Selbstbetrachtungen/.test(l))
    .filter((l) => !/^\|\s*\d+\s*\|$/.test(l));

  const out: ParsedSection[] = [];
  let book = 0;
  let section = 0;
  let buf: string[] = [];

  const flush = () => {
    if (book > 0 && section > 0 && buf.length > 0) {
      out.push({ book, section, text: buf.join(' ').replace(/\s+/g, ' ').trim() });
    }
    buf = [];
  };

  for (const line of lines) {
    if (BOOKS[line] !== undefined) {
      flush();
      book = BOOKS[line];
      section = 0;
      continue;
    }
    if (book === 0) continue; // Titelei/Einleitung verwerfen
    const m = line.match(/^(\d{1,2})\.\s+(.*)$/);
    if (m) {
      flush();
      section = Number(m[1]);
      buf = [m[2]];
    } else if (section > 0 && line.length > 0) {
      buf.push(line);
    }
  }
  flush();
  return out;
}
```

- [ ] **Step 4: Testlauf — muss bestehen**

Run: `npm test -- parsers/__tests__/de` → Expected: PASS.

- [ ] **Step 5: Extraktionsskript schreiben und laufen lassen**

`scripts/extract-de.ts`:
```ts
import { execFileSync } from 'node:child_process';
import { mkdirSync, writeFileSync } from 'node:fs';
import { parseDe } from './parsers/de';

const PDF = 'data-sources/Selbstbetrachtungen-Wittstock-Reclam.pdf';
const raw = execFileSync('pdftotext', ['-layout', '-enc', 'UTF-8', PDF, '-'], {
  encoding: 'utf8',
  maxBuffer: 64 * 1024 * 1024,
});
const sections = parseDe(raw);

const perBook = new Map<number, number>();
for (const s of sections) perBook.set(s.book, (perBook.get(s.book) ?? 0) + 1);
console.log('Abschnitte pro Buch:', Object.fromEntries([...perBook.entries()].sort((a, b) => a[0] - b[0])));
console.log('Gesamt:', sections.length);

mkdirSync('data-sources/extracted', { recursive: true });
writeFileSync('data-sources/extracted/de.json', JSON.stringify(sections, null, 1));
```

Run: `npx tsx scripts/extract-de.ts`
(Falls `pdftotext` fehlt: `sudo dnf install poppler-utils` vorschlagen — nicht selbst installieren, User fragen.)

Expected: 12 Bücher, Gesamt in der Größenordnung 450–500. **Wenn die Format-Annahme nicht stimmt** (z. B. 0 Abschnitte für ein Buch): tatsächliches `pdftotext`-Output für die betroffene Stelle ansehen, Fixture + Regex entsprechend korrigieren, Tests erneut laufen lassen — die Testdatei ist die Dokumentation der echten Formatannahme.

- [ ] **Step 6: Stichprobe und Commit**

Manuell prüfen: `de.json` öffnen, Buch 2 Abschnitt 1 und Buch 12 letzter Abschnitt gegen das PDF (Seiten im Reader) vergleichen — Text vollständig, keine Kopfzeilen-Reste.

```bash
git add scripts data-sources/extracted/de.json && git commit -m "feat: deutscher Text (Wittstock) extrahiert"
```

---

### Task 4: Parser Englisch (Project Gutenberg #2680, Long)

**Files:**
- Create: `scripts/parsers/en.ts`, `scripts/extract-en.ts`
- Test: `scripts/parsers/__tests__/en.test.ts`
- Download (committen): `data-sources/pg2680.txt`
- Output (committen): `data-sources/extracted/en.json`

**Interfaces:**
- Produces: `function parseEn(raw: string): Array<{ book: number; section: number; text: string }>`; `data-sources/extracted/en.json`.
- Format-Annahme: Bücher als Zeile `THE FIRST BOOK` … `THE TWELFTH BOOK` (ggf. mit umgebenden Leerzeilen); Abschnitte beginnen am Zeilenanfang mit römischer Zahl + Punkt (`I. `, `XIV. `); Gutenberg-Header/Footer (`*** START OF …` / `*** END OF …`) abschneiden.

- [ ] **Step 1: Quelldatei laden**

```bash
curl -fsSL https://www.gutenberg.org/cache/epub/2680/pg2680.txt -o data-sources/pg2680.txt
head -c 2000 data-sources/pg2680.txt
```

Format gegen die Annahme prüfen (Buch-Überschriften, Abschnittsnummerierung). Weicht es ab: Annahme im Fixture des nächsten Steps an das reale Format anpassen (die Parserstruktur bleibt gleich).

- [ ] **Step 2: Failing Test mit Fixture**

`scripts/parsers/__tests__/en.test.ts`:
```ts
import { parseEn } from '../en';

const FIXTURE = `*** START OF THE PROJECT GUTENBERG EBOOK MEDITATIONS ***
Introduction text to be discarded.

THE FIRST BOOK

I. Of my grandfather Verus I have learned to be
gentle and meek.

II. Of him that brought me up, not to be fondly
addicted to either of the two great factions.

THE SECOND BOOK

I. Remember how long thou hast already put off these things.

*** END OF THE PROJECT GUTENBERG EBOOK MEDITATIONS ***
`;

describe('parseEn', () => {
  it('erkennt Bücher, römische Abschnittsnummern und schneidet Gutenberg-Rahmen ab', () => {
    const result = parseEn(FIXTURE);
    expect(result).toEqual([
      { book: 1, section: 1, text: 'Of my grandfather Verus I have learned to be gentle and meek.' },
      { book: 1, section: 2, text: 'Of him that brought me up, not to be fondly addicted to either of the two great factions.' },
      { book: 2, section: 1, text: 'Remember how long thou hast already put off these things.' },
    ]);
  });
});
```

- [ ] **Step 3: Testlauf — muss fehlschlagen**

Run: `npm test -- parsers/__tests__/en` → Expected: FAIL.

- [ ] **Step 4: Parser implementieren**

`scripts/parsers/en.ts`:
```ts
const BOOK_WORDS: Record<string, number> = {
  FIRST: 1, SECOND: 2, THIRD: 3, FOURTH: 4, FIFTH: 5, SIXTH: 6,
  SEVENTH: 7, EIGHTH: 8, NINTH: 9, TENTH: 10, ELEVENTH: 11, TWELFTH: 12,
};

function romanToInt(r: string): number {
  const vals: Record<string, number> = { I: 1, V: 5, X: 10, L: 50, C: 100 };
  let total = 0;
  for (let i = 0; i < r.length; i++) {
    const cur = vals[r[i]];
    const next = vals[r[i + 1]] ?? 0;
    total += cur < next ? -cur : cur;
  }
  return total;
}

export interface ParsedSection {
  book: number;
  section: number;
  text: string;
}

export function parseEn(raw: string): ParsedSection[] {
  const start = raw.indexOf('*** START');
  const end = raw.indexOf('*** END');
  const body = raw.slice(start >= 0 ? raw.indexOf('\n', start) + 1 : 0, end >= 0 ? end : raw.length);

  const out: ParsedSection[] = [];
  let book = 0;
  let section = 0;
  let buf: string[] = [];

  const flush = () => {
    if (book > 0 && section > 0 && buf.length > 0) {
      out.push({ book, section, text: buf.join(' ').replace(/\s+/g, ' ').trim() });
    }
    buf = [];
  };

  for (const rawLine of body.split('\n')) {
    const line = rawLine.trim();
    const bookMatch = line.match(/^THE (\w+) BOOK$/);
    if (bookMatch && BOOK_WORDS[bookMatch[1]] !== undefined) {
      flush();
      book = BOOK_WORDS[bookMatch[1]];
      section = 0;
      continue;
    }
    if (book === 0) continue;
    const m = line.match(/^([IVXLC]+)\.\s+(.*)$/);
    if (m) {
      flush();
      section = romanToInt(m[1]);
      buf = [m[2]];
    } else if (section > 0 && line.length > 0) {
      buf.push(line);
    }
  }
  flush();
  return out;
}
```

- [ ] **Step 5: Testlauf — muss bestehen**

Run: `npm test -- parsers/__tests__/en` → Expected: PASS.

- [ ] **Step 6: Extraktionsskript und Lauf**

`scripts/extract-en.ts`:
```ts
import { readFileSync, mkdirSync, writeFileSync } from 'node:fs';
import { parseEn } from './parsers/en';

const raw = readFileSync('data-sources/pg2680.txt', 'utf8');
const sections = parseEn(raw);

const perBook = new Map<number, number>();
for (const s of sections) perBook.set(s.book, (perBook.get(s.book) ?? 0) + 1);
console.log('Abschnitte pro Buch:', Object.fromEntries([...perBook.entries()].sort((a, b) => a[0] - b[0])));
console.log('Gesamt:', sections.length);

mkdirSync('data-sources/extracted', { recursive: true });
writeFileSync('data-sources/extracted/en.json', JSON.stringify(sections, null, 1));
```

Run: `npx tsx scripts/extract-en.ts` → Expected: 12 Bücher, Gesamt 450–500.

- [ ] **Step 7: Commit**

```bash
git add scripts data-sources/pg2680.txt data-sources/extracted/en.json && git commit -m "feat: englischer Text (Long) extrahiert"
```

---

### Task 5: Parser Altgriechisch (PerseusDL TEI-XML)

**Files:**
- Create: `scripts/parsers/grc.ts`, `scripts/extract-grc.ts`
- Test: `scripts/parsers/__tests__/grc.test.ts`
- Download (committen): `data-sources/tlg0562.tlg001.xml`
- Output (committen): `data-sources/extracted/grc.json`

**Interfaces:**
- Produces: `function parseGrc(xml: string): Array<{ book: number; section: number; text: string }>`; `data-sources/extracted/grc.json`.
- Quelle: GitHub `PerseusDL/canonical-greekLit`, Werk `tlg0562.tlg001` (Marcus Aurelius, Τὰ εἰς ἑαυτόν). Exakten Dateinamen (`…perseus-grc*.xml`) über die GitHub-Verzeichnisliste `data/tlg0562/tlg001/` ermitteln.

- [ ] **Step 1: XML laden und Struktur prüfen**

```bash
curl -fsSL "https://api.github.com/repos/PerseusDL/canonical-greekLit/contents/data/tlg0562/tlg001" | grep '"name"'
# dann mit dem gefundenen grc-Dateinamen:
curl -fsSL "https://raw.githubusercontent.com/PerseusDL/canonical-greekLit/master/data/tlg0562/tlg001/<DATEINAME>" -o data-sources/tlg0562.tlg001.xml
grep -o '<div[^>]*subtype="[^"]*"' data-sources/tlg0562.tlg001.xml | sort -u | head
```

Erwartete Struktur: verschachtelte `<div type="textpart" subtype="book" n="…">` mit inneren `<div … subtype="section" n="…">` (oder `chapter`). Tatsächliche `subtype`-Werte notieren und im Parser-Aufruf verwenden.

- [ ] **Step 2: Failing Test mit Fixture**

`scripts/parsers/__tests__/grc.test.ts`:
```ts
import { parseGrc } from '../grc';

const FIXTURE = `<?xml version="1.0" encoding="UTF-8"?>
<TEI xmlns="http://www.tei-c.org/ns/1.0">
 <text><body>
  <div type="textpart" subtype="book" n="1">
   <div type="textpart" subtype="section" n="1">
    <p>Παρὰ τοῦ πάππου Οὐήρου τὸ καλόηθες καὶ ἀόργητον.</p>
   </div>
   <div type="textpart" subtype="section" n="2">
    <p>Παρὰ τῆς δόξης καὶ μνήμης τῆς περὶ τοῦ γεννήσαντος
    τὸ αἰδῆμον καὶ ἀρρενικόν.</p>
   </div>
  </div>
  <div type="textpart" subtype="book" n="2">
   <div type="textpart" subtype="section" n="1">
    <p>Ἕωθεν προλέγειν ἑαυτῷ.</p>
   </div>
  </div>
 </body></text>
</TEI>`;

describe('parseGrc', () => {
  it('liest Buch/Abschnitt aus TEI-Divs und normalisiert Whitespace', () => {
    const result = parseGrc(FIXTURE);
    expect(result).toEqual([
      { book: 1, section: 1, text: 'Παρὰ τοῦ πάππου Οὐήρου τὸ καλόηθες καὶ ἀόργητον.' },
      { book: 1, section: 2, text: 'Παρὰ τῆς δόξης καὶ μνήμης τῆς περὶ τοῦ γεννήσαντος τὸ αἰδῆμον καὶ ἀρρενικόν.' },
      { book: 2, section: 1, text: 'Ἕωθεν προλέγειν ἑαυτῷ.' },
    ]);
  });
});
```

- [ ] **Step 3: Testlauf — muss fehlschlagen**

Run: `npm test -- parsers/__tests__/grc` → Expected: FAIL.

- [ ] **Step 4: Parser implementieren** (regexbasiert, keine XML-Dependency)

`scripts/parsers/grc.ts`:
```ts
export interface ParsedSection {
  book: number;
  section: number;
  text: string;
}

function stripTags(s: string): string {
  return s
    .replace(/<note[\s\S]*?<\/note>/g, ' ')
    .replace(/<[^>]+>/g, ' ')
    .replace(/&amp;/g, '&')
    .replace(/&lt;/g, '<')
    .replace(/&gt;/g, '>')
    .replace(/\s+/g, ' ')
    .trim();
}

export function parseGrc(xml: string): ParsedSection[] {
  const out: ParsedSection[] = [];
  const bookRe = /<div[^>]*subtype="book"[^>]*n="(\d+)"[^>]*>([\s\S]*?)(?=<div[^>]*subtype="book"|<\/body>)/g;
  let bm: RegExpExecArray | null;
  while ((bm = bookRe.exec(xml)) !== null) {
    const book = Number(bm[1]);
    const bookBody = bm[2];
    const secRe = /<div[^>]*subtype="(?:section|chapter)"[^>]*n="(\d+)"[^>]*>([\s\S]*?)(?=<div[^>]*subtype="(?:section|chapter)"|$)/g;
    let sm: RegExpExecArray | null;
    while ((sm = secRe.exec(bookBody)) !== null) {
      const text = stripTags(sm[2]);
      if (text.length > 0) out.push({ book, section: Number(sm[1]), text });
    }
  }
  return out;
}
```

- [ ] **Step 5: Testlauf — muss bestehen**

Run: `npm test -- parsers/__tests__/grc` → Expected: PASS.

- [ ] **Step 6: Extraktionsskript und Lauf**

`scripts/extract-grc.ts`:
```ts
import { readFileSync, mkdirSync, writeFileSync } from 'node:fs';
import { parseGrc } from './parsers/grc';

const raw = readFileSync('data-sources/tlg0562.tlg001.xml', 'utf8');
const sections = parseGrc(raw);

const perBook = new Map<number, number>();
for (const s of sections) perBook.set(s.book, (perBook.get(s.book) ?? 0) + 1);
console.log('Abschnitte pro Buch:', Object.fromEntries([...perBook.entries()].sort((a, b) => a[0] - b[0])));
console.log('Gesamt:', sections.length);

mkdirSync('data-sources/extracted', { recursive: true });
writeFileSync('data-sources/extracted/grc.json', JSON.stringify(sections, null, 1));
```

Run: `npx tsx scripts/extract-grc.ts` → Expected: 12 Bücher, Gesamt 450–500.

- [ ] **Step 7: Commit**

```bash
git add scripts data-sources/tlg0562.tlg001.xml data-sources/extracted/grc.json && git commit -m "feat: griechischer Originaltext extrahiert"
```

---

### Task 6: Merge, Alignment-Report, `data/quotes.json`, `data/SOURCES.md`

**Files:**
- Create: `scripts/build-quotes.ts`, `data-sources/overrides/remap.json`, `data/SOURCES.md`
- Test: `scripts/__tests__/build-quotes.test.ts`
- Output (committen): `data/quotes.json`, `data-sources/alignment-report.txt`

**Interfaces:**
- Consumes: `data-sources/extracted/{de,en,grc}.json` (Format aus Tasks 3–5).
- Produces: `data/quotes.json` = `Quote[]` (Task-2-Interface); nur Abschnitte, die in **allen drei** Sprachen vorliegen; `id` = `"<book>-<section>"`.
- Remap-Format `data-sources/overrides/remap.json`: `{ "en": { "7-14": "7-13" } }` — Quell-ID der Sprache → kanonische ID (kanonisch = griechische Zählung). Initial leer: `{ "de": {}, "en": {}, "grc": {} }`.

- [ ] **Step 1: Failing Test für die Merge-Funktion**

`scripts/__tests__/build-quotes.test.ts`:
```ts
import { mergeSections } from '../build-quotes';

const de = [{ book: 1, section: 1, text: 'de-1-1' }, { book: 1, section: 2, text: 'de-1-2' }];
const en = [{ book: 1, section: 1, text: 'en-1-1' }, { book: 1, section: 3, text: 'en-1-3' }];
const grc = [{ book: 1, section: 1, text: 'grc-1-1' }, { book: 1, section: 2, text: 'grc-1-2' }];

describe('mergeSections', () => {
  it('nimmt nur vollständige Tripel auf und meldet Lücken', () => {
    const { quotes, missing } = mergeSections({ de, en, grc }, { de: {}, en: {}, grc: {} });
    expect(quotes).toEqual([
      { id: '1-1', book: 1, section: 1, texts: { de: 'de-1-1', en: 'en-1-1', grc: 'grc-1-1' } },
    ]);
    expect(missing).toContainEqual({ id: '1-2', lacking: ['en'] });
    expect(missing).toContainEqual({ id: '1-3', lacking: ['de', 'grc'] });
  });

  it('wendet Remaps an', () => {
    const { quotes } = mergeSections(
      { de, en: [{ book: 1, section: 3, text: 'en-eigentlich-1-2' }], grc },
      { de: {}, en: { '1-3': '1-2' }, grc: {} },
    );
    expect(quotes.find((q) => q.id === '1-2')?.texts.en).toBe('en-eigentlich-1-2');
  });
});
```

- [ ] **Step 2: Testlauf — muss fehlschlagen**

Run: `npm test -- build-quotes` → Expected: FAIL.

- [ ] **Step 3: Implementierung**

`scripts/build-quotes.ts`:
```ts
import { readFileSync, writeFileSync } from 'node:fs';

export interface Section { book: number; section: number; text: string }
export type Lang = 'de' | 'en' | 'grc';
export type Remap = Record<Lang, Record<string, string>>;

export interface MergeResult {
  quotes: Array<{ id: string; book: number; section: number; texts: Record<Lang, string> }>;
  missing: Array<{ id: string; lacking: Lang[] }>;
}

export function mergeSections(sources: Record<Lang, Section[]>, remap: Remap): MergeResult {
  const maps: Record<Lang, Map<string, string>> = { de: new Map(), en: new Map(), grc: new Map() };
  for (const lang of ['de', 'en', 'grc'] as Lang[]) {
    for (const s of sources[lang]) {
      const rawId = `${s.book}-${s.section}`;
      const id = remap[lang][rawId] ?? rawId;
      maps[lang].set(id, s.text);
    }
  }
  const allIds = new Set<string>([...maps.de.keys(), ...maps.en.keys(), ...maps.grc.keys()]);
  const quotes: MergeResult['quotes'] = [];
  const missing: MergeResult['missing'] = [];
  const sorted = [...allIds].sort((a, b) => {
    const [ab, as] = a.split('-').map(Number);
    const [bb, bs] = b.split('-').map(Number);
    return ab - bb || as - bs;
  });
  for (const id of sorted) {
    const lacking = (['de', 'en', 'grc'] as Lang[]).filter((l) => !maps[l].has(id));
    if (lacking.length === 0) {
      const [book, section] = id.split('-').map(Number);
      quotes.push({ id, book, section, texts: { de: maps.de.get(id)!, en: maps.en.get(id)!, grc: maps.grc.get(id)! } });
    } else {
      missing.push({ id, lacking });
    }
  }
  return { quotes, missing };
}

// CLI-Teil: nur ausführen, wenn direkt gestartet
if (process.argv[1]?.endsWith('build-quotes.ts')) {
  const load = (l: Lang) => JSON.parse(readFileSync(`data-sources/extracted/${l}.json`, 'utf8')) as Section[];
  const remap = JSON.parse(readFileSync('data-sources/overrides/remap.json', 'utf8')) as Remap;
  const { quotes, missing } = mergeSections({ de: load('de'), en: load('en'), grc: load('grc') }, remap);
  writeFileSync('data/quotes.json', JSON.stringify(quotes));
  const report = [
    `Vollständige Abschnitte: ${quotes.length}`,
    `Unvollständig: ${missing.length}`,
    ...missing.map((m) => `  ${m.id} fehlt in: ${m.lacking.join(', ')}`),
  ].join('\n');
  writeFileSync('data-sources/alignment-report.txt', report + '\n');
  console.log(report);
}
```

- [ ] **Step 4: Testlauf — muss bestehen**

Run: `npm test -- build-quotes` → Expected: PASS.

- [ ] **Step 5: Build laufen lassen und Alignment iterieren**

```bash
mkdir -p data data-sources/overrides
echo '{ "de": {}, "en": {}, "grc": {} }' > data-sources/overrides/remap.json
npx tsx scripts/build-quotes.ts
```

Expected: ≥ 440 vollständige Abschnitte. Report prüfen: Bei systematischen Verschiebungen (eine Sprache konsistent ±1 in einem Buch) Remap-Einträge ergänzen und erneut bauen. Ziel: `Unvollständig` < 15; Rest dokumentiert stehen lassen (nicht erfundene Zuordnungen erzwingen). Stichprobe: 3 zufällige IDs — DE/EN/GRC inhaltlich derselbe Abschnitt?

- [ ] **Step 6: `data/SOURCES.md` schreiben**

```markdown
# Quellen & Lizenzen

Alle Texte sind gemeinfrei (Urheber > 70 Jahre tot bzw. antiker Text).

| Sprache | Quelle | Nachweis |
|---|---|---|
| Deutsch | Albert Wittstock (Übers.), *Des Kaisers Marcus Aurelius Antoninus Selbstbetrachtungen*, Reclam UB 1241 (Erstausgabe 1879; verwendeter Nachdruck 1986). Wittstock † 1903 → gemeinfrei seit 1974. | PDF: `data-sources/Selbstbetrachtungen-Wittstock-Reclam.pdf` (von freismuth.org) |
| Englisch | George Long (Übers.), *The Meditations of Marcus Aurelius* (1862). Long † 1879 → gemeinfrei. | Project Gutenberg #2680, `data-sources/pg2680.txt` |
| Altgriechisch | Marcus Aurelius, *Τὰ εἰς ἑαυτόν*; Edition aus PerseusDL canonical-greekLit (tlg0562.tlg001). | `data-sources/tlg0562.tlg001.xml`, Lizenz der Edition: CC BY-SA (PerseusDL) — Namensnennung in der App-Info nicht nötig, Nachweis hier genügt für unser Nutzungsszenario; bei App-Store-Release erneut prüfen. |

Zählung: kanonisch nach der griechischen Edition; Abweichungen der Übersetzungen sind in `data-sources/overrides/remap.json` dokumentiert. Nicht alignierbare Abschnitte: siehe `data-sources/alignment-report.txt`.
```

(Beim Schreiben: Todesjahre und PerseusDL-Lizenz kurz per Websuche verifizieren und ggf. korrigieren.)

- [ ] **Step 7: Commit**

```bash
git add scripts data data-sources/overrides data-sources/alignment-report.txt && git commit -m "feat: quotes.json aus drei Quellen gebaut, Quellen dokumentiert"
```

---

### Task 7: Theme, Fonts, useTheme

**Files:**
- Create: `theme/tokens.ts`, `theme/ThemeContext.tsx`
- Modify: `app/_layout.tsx`
- Test: `theme/__tests__/tokens.test.ts`

**Interfaces:**
- Produces:
  - `interface ThemeColors { bg: string; card: string; text: string; textSoft: string; accent: string; border: string }`
  - `const palettes: { light: ThemeColors; dark: ThemeColors }`
  - `type ThemePref = 'light' | 'dark' | 'system'`
  - `function resolveTheme(pref: ThemePref, system: 'light' | 'dark' | null | undefined): 'light' | 'dark'`
  - React-Context: `ThemeProvider` + `useTheme(): { colors: ThemeColors; pref: ThemePref; setPref(p: ThemePref): void }`
  - Geladene Fonts: `CormorantGaramond_500Medium`, `CormorantGaramond_500Medium_Italic`, `GFSDidot_400Regular`.

- [ ] **Step 1: Fonts installieren**

```bash
npx expo install expo-font @expo-google-fonts/cormorant-garamond @expo-google-fonts/gfs-didot
```

- [ ] **Step 2: Failing Test für `resolveTheme`**

`theme/__tests__/tokens.test.ts`:
```ts
import { resolveTheme, palettes } from '../tokens';

describe('resolveTheme', () => {
  it('folgt der expliziten Präferenz', () => {
    expect(resolveTheme('light', 'dark')).toBe('light');
    expect(resolveTheme('dark', 'light')).toBe('dark');
  });
  it('folgt bei system dem OS, Fallback light', () => {
    expect(resolveTheme('system', 'dark')).toBe('dark');
    expect(resolveTheme('system', null)).toBe('light');
  });
});

describe('palettes', () => {
  it('definiert beide Paletten vollständig', () => {
    for (const p of [palettes.light, palettes.dark]) {
      for (const v of Object.values(p)) expect(v).toMatch(/^#[0-9A-Fa-f]{6}$/);
    }
  });
});
```

Run: `npm test -- tokens` → Expected: FAIL.

- [ ] **Step 3: Tokens implementieren**

`theme/tokens.ts`:
```ts
export interface ThemeColors {
  bg: string;
  card: string;
  text: string;
  textSoft: string;
  accent: string;
  border: string;
}

export const palettes: { light: ThemeColors; dark: ThemeColors } = {
  light: {
    bg: '#F4EEE1',      // Elfenbein
    card: '#FBF7ED',
    text: '#1B2531',     // Tinte
    textSoft: '#5A6575',
    accent: '#A6763C',   // Bronze
    border: '#E2D9C6',
  },
  dark: {
    bg: '#0F151D',       // tiefes Tintenblau
    card: '#161F2A',
    text: '#EAE2D2',
    textSoft: '#9AA3B0',
    accent: '#C9A264',   // helle Bronze
    border: '#26303D',
  },
};

export type ThemePref = 'light' | 'dark' | 'system';

export function resolveTheme(pref: ThemePref, system: 'light' | 'dark' | null | undefined): 'light' | 'dark' {
  if (pref === 'system') return system ?? 'light';
  return pref;
}

export const fonts = {
  quote: 'CormorantGaramond_500Medium',
  quoteItalic: 'CormorantGaramond_500Medium_Italic',
  greek: 'GFSDidot_400Regular',
};
```

Run: `npm test -- tokens` → Expected: PASS.

- [ ] **Step 4: ThemeContext + Root-Layout**

`theme/ThemeContext.tsx`:
```tsx
import React, { createContext, useContext, useEffect, useState } from 'react';
import { useColorScheme } from 'react-native';
import { palettes, resolveTheme, ThemeColors, ThemePref } from './tokens';
import { getThemePref, setThemePref } from '../lib/settings';

interface ThemeCtx {
  colors: ThemeColors;
  pref: ThemePref;
  setPref: (p: ThemePref) => void;
}

const Ctx = createContext<ThemeCtx>({ colors: palettes.light, pref: 'system', setPref: () => {} });

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const system = useColorScheme();
  const [pref, setPrefState] = useState<ThemePref>('system');

  useEffect(() => {
    getThemePref().then(setPrefState);
  }, []);

  const setPref = (p: ThemePref) => {
    setPrefState(p);
    void setThemePref(p);
  };

  const colors = palettes[resolveTheme(pref, system)];
  return <Ctx.Provider value={{ colors, pref, setPref }}>{children}</Ctx.Provider>;
}

export function useTheme(): ThemeCtx {
  return useContext(Ctx);
}
```

Hinweis: `lib/settings.ts` entsteht in Task 8. Damit dieser Task eigenständig kompiliert, jetzt eine minimale Übergangsdatei anlegen (wird in Task 8 durch die echte ersetzt):

`lib/settings.ts` (Übergang):
```ts
import type { ThemePref } from '../theme/tokens';
export async function getThemePref(): Promise<ThemePref> { return 'system'; }
export async function setThemePref(_p: ThemePref): Promise<void> {}
```

`app/_layout.tsx` ersetzen:
```tsx
import { Stack } from 'expo-router';
import { useFonts, CormorantGaramond_500Medium, CormorantGaramond_500Medium_Italic } from '@expo-google-fonts/cormorant-garamond';
import { GFSDidot_400Regular } from '@expo-google-fonts/gfs-didot';
import { ThemeProvider } from '../theme/ThemeContext';

export default function RootLayout() {
  const [loaded] = useFonts({
    CormorantGaramond_500Medium,
    CormorantGaramond_500Medium_Italic,
    GFSDidot_400Regular,
  });
  if (!loaded) return null;
  return (
    <ThemeProvider>
      <Stack screenOptions={{ headerShown: false }}>
        <Stack.Screen name="index" />
        <Stack.Screen name="settings" options={{ presentation: 'modal' }} />
      </Stack>
    </ThemeProvider>
  );
}
```

- [ ] **Step 5: Verifizieren und Commit**

Run: `npx tsc --noEmit` und `npm test` → Expected: grün.

```bash
git add theme lib app && git commit -m "feat: Theme-Tokens, Fonts und ThemeProvider"
```

---

### Task 8: Storage & Settings (TDD)

**Files:**
- Create: `lib/storage.ts`
- Modify (ersetzen): `lib/settings.ts`
- Test: `lib/__tests__/settings.test.ts`

**Interfaces:**
- Produces:
  - `lib/storage.ts`: `getItem(key: string): Promise<string | null>`, `setItem(key, value): Promise<void>`, `deleteItem(key): Promise<void>` — nativ `expo-secure-store`, Web `localStorage` (Weiche über `Platform.OS === 'web'`).
  - `lib/settings.ts`: `getQuoteLang(): Promise<QuoteLang>` (Default `'de'`), `setQuoteLang(l)`, `getThemePref(): Promise<ThemePref>` (Default `'system'`), `setThemePref(p)`, `getAnthropicKey(): Promise<string | null>`, `setAnthropicKey(k: string)`, `deleteAnthropicKey()`.
  - Storage-Keys: `aurelius.quoteLang`, `aurelius.theme`, `aurelius.anthropicKey`.

- [ ] **Step 1: Installieren**

```bash
npx expo install expo-secure-store
```

- [ ] **Step 2: Failing Tests (storage gemockt)**

`lib/__tests__/settings.test.ts`:
```ts
const store = new Map<string, string>();
jest.mock('../storage', () => ({
  getItem: jest.fn(async (k: string) => store.get(k) ?? null),
  setItem: jest.fn(async (k: string, v: string) => void store.set(k, v)),
  deleteItem: jest.fn(async (k: string) => void store.delete(k)),
}));

import {
  getQuoteLang, setQuoteLang, getThemePref, setThemePref,
  getAnthropicKey, setAnthropicKey, deleteAnthropicKey,
} from '../settings';

beforeEach(() => store.clear());

describe('settings', () => {
  it('liefert Defaults', async () => {
    expect(await getQuoteLang()).toBe('de');
    expect(await getThemePref()).toBe('system');
    expect(await getAnthropicKey()).toBeNull();
  });

  it('persistiert Werte', async () => {
    await setQuoteLang('grc');
    await setThemePref('dark');
    await setAnthropicKey('sk-ant-test');
    expect(await getQuoteLang()).toBe('grc');
    expect(await getThemePref()).toBe('dark');
    expect(await getAnthropicKey()).toBe('sk-ant-test');
  });

  it('ignoriert ungültige gespeicherte Werte (Default)', async () => {
    store.set('aurelius.quoteLang', 'xx');
    expect(await getQuoteLang()).toBe('de');
  });

  it('löscht den Key', async () => {
    await setAnthropicKey('sk-ant-test');
    await deleteAnthropicKey();
    expect(await getAnthropicKey()).toBeNull();
  });
});
```

Run: `npm test -- settings` → Expected: FAIL.

- [ ] **Step 3: Implementierung**

`lib/storage.ts`:
```ts
import { Platform } from 'react-native';
import * as SecureStore from 'expo-secure-store';

export async function getItem(key: string): Promise<string | null> {
  if (Platform.OS === 'web') return globalThis.localStorage?.getItem(key) ?? null;
  return SecureStore.getItemAsync(key);
}

export async function setItem(key: string, value: string): Promise<void> {
  if (Platform.OS === 'web') {
    globalThis.localStorage?.setItem(key, value);
    return;
  }
  await SecureStore.setItemAsync(key, value);
}

export async function deleteItem(key: string): Promise<void> {
  if (Platform.OS === 'web') {
    globalThis.localStorage?.removeItem(key);
    return;
  }
  await SecureStore.deleteItemAsync(key);
}
```

`lib/settings.ts` (ersetzt die Übergangsdatei aus Task 7 vollständig):
```ts
import { getItem, setItem, deleteItem } from './storage';
import type { QuoteLang } from './quotes';
import type { ThemePref } from '../theme/tokens';

const K_LANG = 'aurelius.quoteLang';
const K_THEME = 'aurelius.theme';
const K_KEY = 'aurelius.anthropicKey';

const LANGS: QuoteLang[] = ['de', 'en', 'grc'];
const THEMES: ThemePref[] = ['light', 'dark', 'system'];

export async function getQuoteLang(): Promise<QuoteLang> {
  const v = await getItem(K_LANG);
  return LANGS.includes(v as QuoteLang) ? (v as QuoteLang) : 'de';
}

export async function setQuoteLang(l: QuoteLang): Promise<void> {
  await setItem(K_LANG, l);
}

export async function getThemePref(): Promise<ThemePref> {
  const v = await getItem(K_THEME);
  return THEMES.includes(v as ThemePref) ? (v as ThemePref) : 'system';
}

export async function setThemePref(p: ThemePref): Promise<void> {
  await setItem(K_THEME, p);
}

export async function getAnthropicKey(): Promise<string | null> {
  const v = await getItem(K_KEY);
  return v && v.length > 0 ? v : null;
}

export async function setAnthropicKey(key: string): Promise<void> {
  await setItem(K_KEY, key.trim());
}

export async function deleteAnthropicKey(): Promise<void> {
  await deleteItem(K_KEY);
}
```

- [ ] **Step 4: Testlauf und Commit**

Run: `npm test` → Expected: alle grün. `npx tsc --noEmit` → grün.

```bash
git add lib && git commit -m "feat: Storage-Abstraktion und Settings"
```

---

### Task 9: KI-Provider (BYOK Claude + Fallback-Client, TDD für Auswahl & Prompt)

**Files:**
- Create: `lib/ai/prompt.ts`, `lib/ai/errors.ts`, `lib/ai/anthropic.ts`, `lib/ai/gemini.ts`, `lib/ai/index.ts`
- Test: `lib/ai/__tests__/prompt.test.ts`, `lib/ai/__tests__/select.test.ts`

**Interfaces:**
- Consumes: `getAnthropicKey()` (Task 8), `Quote`/`QuoteLang`/`formatReference` (Task 2).
- Produces:
  - `buildExplainPrompt(text: string, reference: string): string`, `EXPLAIN_SYSTEM: string`
  - `class ExplainError extends Error { kind: 'offline' | 'auth' | 'rate_limited' | 'server' }`
  - `explainWithClaude(apiKey: string, quote: Quote, lang: QuoteLang): AsyncIterable<string>`
  - `explainWithGemini(quote: Quote, lang: QuoteLang): AsyncIterable<string>` (ruft `EXPLAIN_URL` auf)
  - `getExplainStream(quote: Quote, lang: QuoteLang): Promise<AsyncIterable<string>>` — Key vorhanden → Claude, sonst Gemini
  - `EXPLAIN_URL`: aus `process.env.EXPO_PUBLIC_EXPLAIN_URL`, Default `'/api/explain'` (funktioniert im Web-Deploy auf derselben Domain).

- [ ] **Step 1: SDK installieren**

```bash
npm i @anthropic-ai/sdk
```

- [ ] **Step 2: Failing Tests**

`lib/ai/__tests__/prompt.test.ts`:
```ts
import { buildExplainPrompt, EXPLAIN_SYSTEM } from '../prompt';

describe('buildExplainPrompt', () => {
  it('enthält Text und Stellenangabe', () => {
    const p = buildExplainPrompt('Der Text.', 'Buch IV, 7');
    expect(p).toContain('Der Text.');
    expect(p).toContain('Buch IV, 7');
    expect(p).toMatch(/120–180 Wörtern/);
  });
  it('System-Prompt ist deutsch und nicht leer', () => {
    expect(EXPLAIN_SYSTEM.length).toBeGreaterThan(20);
  });
});
```

`lib/ai/__tests__/select.test.ts`:
```ts
const mockKey = jest.fn();
jest.mock('../../settings', () => ({ getAnthropicKey: () => mockKey() }));

const claude = jest.fn();
const gemini = jest.fn();
jest.mock('../anthropic', () => ({ explainWithClaude: (...a: unknown[]) => claude(...a) }));
jest.mock('../gemini', () => ({ explainWithGemini: (...a: unknown[]) => gemini(...a) }));

import { getExplainStream } from '../index';

const quote = { id: '1-1', book: 1, section: 1, texts: { de: 'x', en: 'y', grc: 'z' } };

describe('getExplainStream', () => {
  it('nutzt Claude, wenn ein Key hinterlegt ist', async () => {
    mockKey.mockResolvedValue('sk-ant-abc');
    await getExplainStream(quote, 'de');
    expect(claude).toHaveBeenCalledWith('sk-ant-abc', quote, 'de');
    expect(gemini).not.toHaveBeenCalled();
  });
  it('fällt ohne Key auf Gemini zurück', async () => {
    claude.mockClear(); gemini.mockClear();
    mockKey.mockResolvedValue(null);
    await getExplainStream(quote, 'de');
    expect(gemini).toHaveBeenCalledWith(quote, 'de');
    expect(claude).not.toHaveBeenCalled();
  });
});
```

`lib/ai/__tests__/gemini.test.ts` (Fehlerpfade der KI-Schicht, Spec §9):
```ts
const mockFetch = jest.fn();
jest.mock('expo/fetch', () => ({ fetch: (...a: unknown[]) => mockFetch(...a) }));

import { explainWithGemini } from '../gemini';
import { ExplainError } from '../errors';

const quote = { id: '1-1', book: 1, section: 1, texts: { de: 'x', en: 'y', grc: 'z' } };

async function caught(): Promise<unknown> {
  try {
    for await (const _ of explainWithGemini(quote, 'de')) { /* verbrauchen */ }
    return null;
  } catch (e) {
    return e;
  }
}

describe('explainWithGemini Fehlerpfade', () => {
  it('Netzfehler → offline', async () => {
    mockFetch.mockRejectedValueOnce(new Error('net down'));
    const e = await caught();
    expect(e).toBeInstanceOf(ExplainError);
    expect((e as ExplainError).kind).toBe('offline');
  });
  it('429 → rate_limited', async () => {
    mockFetch.mockResolvedValueOnce({ ok: false, status: 429 });
    expect(((await caught()) as ExplainError).kind).toBe('rate_limited');
  });
  it('500 → server', async () => {
    mockFetch.mockResolvedValueOnce({ ok: false, status: 500 });
    expect(((await caught()) as ExplainError).kind).toBe('server');
  });
});
```

Run: `npm test -- ai` → Expected: FAIL.

- [ ] **Step 3: Implementierung**

`lib/ai/prompt.ts`:
```ts
export const EXPLAIN_SYSTEM =
  'Du bist ein kundiger, nüchterner Begleiter durch Marc Aurels Selbstbetrachtungen. ' +
  'Du erklärst klar, konkret und ohne Kitsch — für interessierte Laien.';

export function buildExplainPrompt(text: string, reference: string): string {
  return (
    `Passage aus Marc Aurels »Selbstbetrachtungen« (${reference}):\n\n` +
    `»${text}«\n\n` +
    'Erkläre diese Passage auf Deutsch in 120–180 Wörtern: zuerst in einem Satz den Kerngedanken, ' +
    'dann kurz den stoischen Hintergrund, zuletzt eine konkrete Anwendung im heutigen Alltag. ' +
    'Antworte direkt ohne Vorspann und ohne Überschriften.'
  );
}
```

`lib/ai/errors.ts`:
```ts
export type ExplainErrorKind = 'offline' | 'auth' | 'rate_limited' | 'server';

export class ExplainError extends Error {
  constructor(public kind: ExplainErrorKind, message?: string) {
    super(message ?? kind);
    this.name = 'ExplainError';
  }
}
```

`lib/ai/anthropic.ts`:
```ts
import Anthropic from '@anthropic-ai/sdk';
import { fetch as expoFetch } from 'expo/fetch';
import { Platform } from 'react-native';
import type { Quote, QuoteLang } from '../quotes';
import { formatReference } from '../quotes';
import { buildExplainPrompt, EXPLAIN_SYSTEM } from './prompt';
import { ExplainError } from './errors';

export async function* explainWithClaude(apiKey: string, quote: Quote, lang: QuoteLang): AsyncIterable<string> {
  const client = new Anthropic({
    apiKey,
    dangerouslyAllowBrowser: true,
    // RN-fetch kann keine Streams; expo/fetch ist WinterCG-konform.
    fetch: (Platform.OS === 'web' ? globalThis.fetch : (expoFetch as unknown)) as typeof globalThis.fetch,
  });
  try {
    const stream = client.messages.stream({
      model: 'claude-opus-5',
      max_tokens: 1024,
      system: EXPLAIN_SYSTEM,
      messages: [{ role: 'user', content: buildExplainPrompt(quote.texts[lang], formatReference(quote)) }],
    });
    for await (const event of stream) {
      if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
        yield event.delta.text;
      }
    }
  } catch (err) {
    if (err instanceof Anthropic.APIConnectionError) throw new ExplainError('offline');
    if (err instanceof Anthropic.AuthenticationError) throw new ExplainError('auth');
    if (err instanceof Anthropic.RateLimitError) throw new ExplainError('rate_limited');
    if (err instanceof Anthropic.APIError) throw new ExplainError('server', String(err.status));
    throw err;
  }
}
```

`lib/ai/gemini.ts`:
```ts
import { fetch as expoFetch } from 'expo/fetch';
import { Platform } from 'react-native';
import type { Quote, QuoteLang } from '../quotes';
import { formatReference } from '../quotes';
import { ExplainError } from './errors';

export const EXPLAIN_URL = process.env.EXPO_PUBLIC_EXPLAIN_URL ?? '/api/explain';

export async function* explainWithGemini(quote: Quote, lang: QuoteLang): AsyncIterable<string> {
  const f = (Platform.OS === 'web' ? globalThis.fetch : (expoFetch as unknown)) as typeof globalThis.fetch;
  let resp: Response;
  try {
    resp = await f(EXPLAIN_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text: quote.texts[lang], reference: formatReference(quote) }),
    });
  } catch {
    throw new ExplainError('offline');
  }
  if (resp.status === 429) throw new ExplainError('rate_limited');
  if (!resp.ok) throw new ExplainError('server', String(resp.status));
  if (!resp.body) throw new ExplainError('server', 'kein Stream');
  const reader = resp.body.getReader();
  const decoder = new TextDecoder();
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    yield decoder.decode(value, { stream: true });
  }
}
```

`lib/ai/index.ts`:
```ts
import type { Quote, QuoteLang } from '../quotes';
import { getAnthropicKey } from '../settings';
import { explainWithClaude } from './anthropic';
import { explainWithGemini } from './gemini';

export { ExplainError } from './errors';

export async function getExplainStream(quote: Quote, lang: QuoteLang): Promise<AsyncIterable<string>> {
  const key = await getAnthropicKey();
  if (key) return explainWithClaude(key, quote, lang);
  return explainWithGemini(quote, lang);
}
```

- [ ] **Step 4: Testlauf und Commit**

Run: `npm test -- ai` → Expected: PASS. `npx tsc --noEmit` → grün.

```bash
git add lib/ai && git commit -m "feat: KI-Provider (Claude BYOK, Gemini-Fallback, Auswahl-Logik)"
```

---

### Task 10: Vercel-Function `/api/explain`

**Files:**
- Create: `api/explain.ts`, `vercel.json`

**Interfaces:**
- Consumes: `buildExplainPrompt`, `EXPLAIN_SYSTEM` aus `lib/ai/prompt.ts` (relativer Import).
- Produces: `POST /api/explain` mit Body `{ text: string, reference: string }` → antwortet `200 text/plain` als Chunk-Stream; `400` bei ungültigem Body, `405` bei falscher Methode, `429` bei Gemini-Quota, `500` sonst. Env-Var serverseitig: `GEMINI_API_KEY`.

- [ ] **Step 1: Dependency installieren, Modellname verifizieren**

```bash
npm i @google/genai
```

Per Websuche verifizieren: aktueller Name des Gemini-Free-Tier-tauglichen Modells (Annahme: `gemini-2.5-flash`) und dass der Free-Tier existiert. Ergebnis als Kommentar in `api/explain.ts` festhalten. Falls kein Free-Tier mehr existiert: Task trotzdem fertigstellen (Function funktioniert mit bezahltem Key), User informieren.

- [ ] **Step 2: Function implementieren**

`api/explain.ts`:
```ts
import type { VercelRequest, VercelResponse } from '@vercel/node';
import { GoogleGenAI } from '@google/genai';
import { buildExplainPrompt, EXPLAIN_SYSTEM } from '../lib/ai/prompt';

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') {
    res.status(405).json({ error: 'nur POST' });
    return;
  }
  const { text, reference } = (req.body ?? {}) as { text?: unknown; reference?: unknown };
  if (typeof text !== 'string' || text.length === 0 || text.length > 8000 || typeof reference !== 'string') {
    res.status(400).json({ error: 'ungültiger Body' });
    return;
  }
  const apiKey = process.env.GEMINI_API_KEY;
  if (!apiKey) {
    res.status(500).json({ error: 'GEMINI_API_KEY fehlt' });
    return;
  }
  const ai = new GoogleGenAI({ apiKey });
  try {
    const stream = await ai.models.generateContentStream({
      model: 'gemini-2.5-flash',
      contents: buildExplainPrompt(text, reference),
      config: { systemInstruction: EXPLAIN_SYSTEM, maxOutputTokens: 1024 },
    });
    res.setHeader('Content-Type', 'text/plain; charset=utf-8');
    res.setHeader('Cache-Control', 'no-store');
    for await (const chunk of stream) {
      if (chunk.text) res.write(chunk.text);
    }
    res.end();
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    if (/429|quota|RESOURCE_EXHAUSTED/i.test(msg)) {
      res.status(429).json({ error: 'Kontingent erschöpft' });
      return;
    }
    res.status(500).json({ error: 'Gemini-Fehler' });
  }
}
```

`npm i -D @vercel/node` für die Typen.

- [ ] **Step 3: `vercel.json`**

```json
{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist"
}
```

- [ ] **Step 4: Verifizieren und Commit**

Run: `npx tsc --noEmit` → grün (die Function wird mitgeprüft). Der Netz-Test erfolgt in Task 13 nach dem Deploy.

```bash
git add api vercel.json package.json package-lock.json && git commit -m "feat: Vercel-Function für Gemini-Fallback"
```

---

### Task 11: UI Home (QuoteCard, Sprachwahl, Animation, Streaming)

**Files:**
- Create: `components/QuoteCard.tsx`, `components/StreamingText.tsx`, `components/Segmented.tsx`
- Modify (ersetzen): `app/index.tsx`

**Interfaces:**
- Consumes: `data/quotes.json`, `ShuffleBag`, `formatReference`, `getExplainStream`, `ExplainError`, `useTheme`, `fonts`, Settings.
- Produces:
  - `Segmented<T extends string>({ options: Array<{ value: T; label: string }>, value: T, onChange(v: T): void })`
  - `QuoteCard({ quote: Quote; lang: QuoteLang; onPress(): void })` — scrollbarer Text, Serifenschrift (`grc` → `fonts.greek`), Stellenangabe in Bronze-Kapitälchen
  - `StreamingText({ stream: AsyncIterable<string> | null; onDone(): void; onError(e: unknown): void })` — hängt Chunks an, blinkender Cursor solange aktiv.

- [ ] **Step 1: `Segmented` implementieren**

`components/Segmented.tsx`:
```tsx
import React from 'react';
import { Pressable, Text, View, StyleSheet } from 'react-native';
import { useTheme } from '../theme/ThemeContext';

interface Props<T extends string> {
  options: Array<{ value: T; label: string }>;
  value: T;
  onChange: (v: T) => void;
}

export function Segmented<T extends string>({ options, value, onChange }: Props<T>) {
  const { colors } = useTheme();
  return (
    <View style={[styles.row, { borderColor: colors.border, backgroundColor: colors.card }]}>
      {options.map((o) => {
        const active = o.value === value;
        return (
          <Pressable
            key={o.value}
            onPress={() => onChange(o.value)}
            style={[styles.seg, active && { backgroundColor: colors.accent }]}
            accessibilityRole="button"
            accessibilityState={{ selected: active }}
          >
            <Text style={{ color: active ? colors.card : colors.textSoft, fontSize: 13, fontWeight: '600' }}>
              {o.label}
            </Text>
          </Pressable>
        );
      })}
    </View>
  );
}

const styles = StyleSheet.create({
  row: { flexDirection: 'row', borderWidth: 1, borderRadius: 999, padding: 3, gap: 2 },
  seg: { paddingHorizontal: 14, paddingVertical: 6, borderRadius: 999 },
});
```

- [ ] **Step 2: `QuoteCard` implementieren**

`components/QuoteCard.tsx`:
```tsx
import React from 'react';
import { Pressable, ScrollView, Text, StyleSheet } from 'react-native';
import type { Quote, QuoteLang } from '../lib/quotes';
import { formatReference } from '../lib/quotes';
import { useTheme } from '../theme/ThemeContext';
import { fonts } from '../theme/tokens';

interface Props {
  quote: Quote;
  lang: QuoteLang;
  onPress: () => void;
}

export function QuoteCard({ quote, lang, onPress }: Props) {
  const { colors } = useTheme();
  const isGreek = lang === 'grc';
  return (
    <Pressable
      onPress={onPress}
      accessibilityRole="button"
      accessibilityHint="Tippen für den nächsten Abschnitt"
      style={[styles.card, { backgroundColor: colors.card, borderColor: colors.border }]}
    >
      <ScrollView style={styles.scroll} showsVerticalScrollIndicator={false}>
        <Text
          style={{
            fontFamily: isGreek ? fonts.greek : fonts.quote,
            fontSize: isGreek ? 22 : 24,
            lineHeight: isGreek ? 34 : 36,
            color: colors.text,
          }}
        >
          {quote.texts[lang]}
        </Text>
      </ScrollView>
      <Text style={[styles.ref, { color: colors.accent }]}>
        {formatReference(quote).toUpperCase()}
      </Text>
    </Pressable>
  );
}

const styles = StyleSheet.create({
  card: { borderWidth: 1, borderRadius: 20, padding: 24, maxHeight: 420, minHeight: 220 },
  scroll: { flexGrow: 0 },
  ref: { marginTop: 16, fontSize: 12, letterSpacing: 2, fontWeight: '600' },
});
```

- [ ] **Step 3: `StreamingText` implementieren**

`components/StreamingText.tsx`:
```tsx
import React, { useEffect, useRef, useState } from 'react';
import { Animated, Text } from 'react-native';
import { useTheme } from '../theme/ThemeContext';

interface Props {
  stream: AsyncIterable<string> | null;
  onDone: () => void;
  onError: (e: unknown) => void;
}

export function StreamingText({ stream, onDone, onError }: Props) {
  const { colors } = useTheme();
  const [text, setText] = useState('');
  const [running, setRunning] = useState(false);
  const cursor = useRef(new Animated.Value(1)).current;

  useEffect(() => {
    if (!stream) return;
    let cancelled = false;
    setText('');
    setRunning(true);
    (async () => {
      try {
        for await (const chunk of stream) {
          if (cancelled) return;
          setText((t) => t + chunk);
        }
        if (!cancelled) onDone();
      } catch (e) {
        if (!cancelled) onError(e);
      } finally {
        if (!cancelled) setRunning(false);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, [stream]);

  useEffect(() => {
    const loop = Animated.loop(
      Animated.sequence([
        Animated.timing(cursor, { toValue: 0, duration: 450, useNativeDriver: true }),
        Animated.timing(cursor, { toValue: 1, duration: 450, useNativeDriver: true }),
      ]),
    );
    if (running) loop.start();
    return () => loop.stop();
  }, [running]);

  if (!stream && text.length === 0) return null;
  return (
    <Text style={{ color: colors.text, fontSize: 16, lineHeight: 26 }}>
      {text}
      {running && <Animated.Text style={{ opacity: cursor, color: colors.accent }}>▌</Animated.Text>}
    </Text>
  );
}
```

- [ ] **Step 4: Home-Screen zusammensetzen**

`app/index.tsx` ersetzen:
```tsx
import React, { useEffect, useMemo, useRef, useState } from 'react';
import { Animated, Pressable, ScrollView, Text, View, StyleSheet } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Link } from 'expo-router';
import quotesData from '../data/quotes.json';
import type { Quote, QuoteLang } from '../lib/quotes';
import { ShuffleBag } from '../lib/quotes';
import { getQuoteLang, setQuoteLang } from '../lib/settings';
import { getExplainStream, ExplainError } from '../lib/ai';
import { useTheme } from '../theme/ThemeContext';
import { QuoteCard } from '../components/QuoteCard';
import { Segmented } from '../components/Segmented';
import { StreamingText } from '../components/StreamingText';

const QUOTES = quotesData as Quote[];

const ERROR_COPY: Record<string, string> = {
  offline: 'Keine Verbindung — die Erklärung braucht Internet.',
  auth: 'Der API-Key wurde abgelehnt. Prüfe ihn in den Einstellungen.',
  rate_limited: 'Gerade ausgelastet. Versuch es gleich nochmal — oder hinterlege einen eigenen Key.',
  server: 'Da ging etwas schief. Versuch es nochmal.',
};

export default function Home() {
  const { colors } = useTheme();
  const bag = useMemo(() => new ShuffleBag(QUOTES.map((q) => q.id)), []);
  const byId = useMemo(() => new Map(QUOTES.map((q) => [q.id, q])), []);
  const [quote, setQuote] = useState<Quote>(() => byId.get(bag.next())!);
  const [lang, setLang] = useState<QuoteLang>('de');
  const [stream, setStream] = useState<AsyncIterable<string> | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  const fade = useRef(new Animated.Value(1)).current;

  useEffect(() => {
    getQuoteLang().then(setLang);
  }, []);

  const nextQuote = () => {
    setStream(null);
    setError(null);
    Animated.timing(fade, { toValue: 0, duration: 150, useNativeDriver: true }).start(() => {
      setQuote(byId.get(bag.next())!);
      Animated.timing(fade, { toValue: 1, duration: 250, useNativeDriver: true }).start();
    });
  };

  const changeLang = (l: QuoteLang) => {
    setLang(l);
    void setQuoteLang(l);
  };

  const explain = async () => {
    setError(null);
    setBusy(true);
    try {
      setStream(await getExplainStream(quote, lang));
    } catch (e) {
      setError(e instanceof ExplainError ? ERROR_COPY[e.kind] : ERROR_COPY.server);
      setBusy(false);
    }
  };

  const onStreamError = (e: unknown) => {
    setStream(null);
    setBusy(false);
    setError(e instanceof ExplainError ? ERROR_COPY[e.kind] : ERROR_COPY.server);
  };

  return (
    <SafeAreaView style={[styles.safe, { backgroundColor: colors.bg }]}>
      <View style={styles.header}>
        <Text style={[styles.brand, { color: colors.textSoft }]}>AURELIUS</Text>
        <Link href="/settings" accessibilityLabel="Einstellungen">
          <Text style={{ color: colors.accent, fontSize: 22 }}>⚙</Text>
        </Link>
      </View>
      <ScrollView contentContainerStyle={styles.content}>
        <Animated.View style={{ opacity: fade }}>
          <QuoteCard quote={quote} lang={lang} onPress={nextQuote} />
        </Animated.View>
        <View style={styles.controls}>
          <Segmented<QuoteLang>
            options={[
              { value: 'de', label: 'Deutsch' },
              { value: 'en', label: 'English' },
              { value: 'grc', label: 'Ἑλληνικά' },
            ]}
            value={lang}
            onChange={changeLang}
          />
          <Pressable
            onPress={explain}
            disabled={busy}
            style={[styles.explainBtn, { backgroundColor: colors.accent }]}
            accessibilityRole="button"
          >
            <Text style={{ color: colors.bg, fontWeight: '700' }}>Erklären</Text>
          </Pressable>
        </View>
        {error && <Text style={{ color: colors.accent, marginTop: 12 }}>{error}</Text>}
        <View style={{ marginTop: 16 }}>
          <StreamingText
            stream={stream}
            onDone={() => setBusy(false)}
            onError={onStreamError}
          />
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: { flex: 1 },
  header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingHorizontal: 20, paddingVertical: 12 },
  brand: { fontSize: 13, letterSpacing: 4, fontWeight: '600' },
  content: { padding: 20, paddingBottom: 48 },
  controls: { marginTop: 20, gap: 12, alignItems: 'center' },
  explainBtn: { paddingHorizontal: 28, paddingVertical: 12, borderRadius: 999 },
});
```

`tsconfig.json`: sicherstellen, dass `"resolveJsonModule": true` gesetzt ist (Expo-Default hat es meist).

- [ ] **Step 5: Smoke-Test im Browser**

Run: `npx expo start --web` — prüfen: Zitat sichtbar, Tap wechselt mit Überblendung, Sprachumschaltung (griechischer Text in GFS Didot, Diakritika korrekt), „Erklären" ohne Key/Function → Fehlermeldung erscheint (erwartet, Function ist noch nicht deployt).

- [ ] **Step 6: Commit**

```bash
git add app components tsconfig.json && git commit -m "feat: Home-Screen mit QuoteCard, Sprachwahl und Streaming-UI"
```

---

### Task 12: UI Settings

**Files:**
- Modify (ersetzen): `app/settings.tsx`

**Interfaces:**
- Consumes: Settings-Funktionen (Task 8), `useTheme` (Task 7), `Segmented` (Task 11).

- [ ] **Step 1: Screen implementieren**

`app/settings.tsx` ersetzen:
```tsx
import React, { useEffect, useState } from 'react';
import { Platform, Pressable, ScrollView, Text, TextInput, View, StyleSheet } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Link } from 'expo-router';
import type { QuoteLang } from '../lib/quotes';
import type { ThemePref } from '../theme/tokens';
import {
  getQuoteLang, setQuoteLang, getAnthropicKey, setAnthropicKey, deleteAnthropicKey,
} from '../lib/settings';
import { useTheme } from '../theme/ThemeContext';
import { Segmented } from '../components/Segmented';

export default function Settings() {
  const { colors, pref, setPref } = useTheme();
  const [lang, setLang] = useState<QuoteLang>('de');
  const [keyInput, setKeyInput] = useState('');
  const [hasKey, setHasKey] = useState(false);
  const [saved, setSaved] = useState(false);

  useEffect(() => {
    getQuoteLang().then(setLang);
    getAnthropicKey().then((k) => setHasKey(k !== null));
  }, []);

  const saveKey = async () => {
    if (keyInput.trim().length === 0) return;
    await setAnthropicKey(keyInput);
    setKeyInput('');
    setHasKey(true);
    setSaved(true);
    setTimeout(() => setSaved(false), 2000);
  };

  const removeKey = async () => {
    await deleteAnthropicKey();
    setHasKey(false);
  };

  const section = (title: string, children: React.ReactNode) => (
    <View style={styles.section}>
      <Text style={[styles.sectionTitle, { color: colors.accent }]}>{title.toUpperCase()}</Text>
      {children}
    </View>
  );

  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: colors.bg }}>
      <ScrollView contentContainerStyle={styles.content}>
        <View style={styles.header}>
          <Text style={[styles.title, { color: colors.text }]}>Einstellungen</Text>
          <Link href="/" accessibilityLabel="Zurück">
            <Text style={{ color: colors.accent, fontSize: 16 }}>Fertig</Text>
          </Link>
        </View>

        {section('Zitat-Sprache', (
          <Segmented<QuoteLang>
            options={[
              { value: 'de', label: 'Deutsch' },
              { value: 'en', label: 'English' },
              { value: 'grc', label: 'Ἑλληνικά' },
            ]}
            value={lang}
            onChange={(l) => { setLang(l); void setQuoteLang(l); }}
          />
        ))}

        {section('Erscheinungsbild', (
          <Segmented<ThemePref>
            options={[
              { value: 'light', label: 'Hell' },
              { value: 'dark', label: 'Dunkel' },
              { value: 'system', label: 'System' },
            ]}
            value={pref}
            onChange={setPref}
          />
        ))}

        {section('KI-Erklärung', (
          <View style={{ gap: 10 }}>
            <Text style={{ color: colors.textSoft, fontSize: 14, lineHeight: 21 }}>
              Mit eigenem Anthropic-API-Key nutzt die App Claude direkt von deinem Gerät.
              Ohne Key springt der eingebaute Gratis-Modus ein (sofern verfügbar).
            </Text>
            {hasKey ? (
              <View style={{ flexDirection: 'row', alignItems: 'center', gap: 12 }}>
                <Text style={{ color: colors.text }}>Key hinterlegt ✓</Text>
                <Pressable onPress={removeKey} accessibilityRole="button">
                  <Text style={{ color: colors.accent }}>Löschen</Text>
                </Pressable>
              </View>
            ) : (
              <View style={{ gap: 8 }}>
                <TextInput
                  value={keyInput}
                  onChangeText={setKeyInput}
                  placeholder="sk-ant-…"
                  placeholderTextColor={colors.textSoft}
                  secureTextEntry
                  autoCapitalize="none"
                  autoCorrect={false}
                  style={[styles.input, { color: colors.text, borderColor: colors.border, backgroundColor: colors.card }]}
                />
                <Pressable
                  onPress={saveKey}
                  style={[styles.saveBtn, { backgroundColor: colors.accent }]}
                  accessibilityRole="button"
                >
                  <Text style={{ color: colors.bg, fontWeight: '700' }}>Key speichern</Text>
                </Pressable>
              </View>
            )}
            {saved && <Text style={{ color: colors.accent }}>Gespeichert.</Text>}
            {Platform.OS === 'web' && (
              <Text style={{ color: colors.textSoft, fontSize: 12 }}>
                Hinweis: Im Browser wird der Key unverschlüsselt im localStorage abgelegt.
              </Text>
            )}
          </View>
        ))}
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  content: { padding: 20, gap: 8 },
  header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 },
  title: { fontSize: 24, fontWeight: '700' },
  section: { marginTop: 20, gap: 10, alignItems: 'flex-start' },
  sectionTitle: { fontSize: 12, letterSpacing: 2, fontWeight: '600' },
  input: { borderWidth: 1, borderRadius: 12, paddingHorizontal: 14, paddingVertical: 10, alignSelf: 'stretch' },
  saveBtn: { paddingHorizontal: 20, paddingVertical: 10, borderRadius: 999, alignSelf: 'flex-start' },
});
```

- [ ] **Step 2: Smoke-Test**

Run: `npx expo start --web` — Einstellungen öffnen: Sprache und Theme umschalten (wirken sofort und überleben Reload), Key speichern/löschen. Mit gespeichertem echtem Key zurück auf Home → „Erklären" → Text streamt.

- [ ] **Step 3: Commit**

```bash
git add app && git commit -m "feat: Einstellungs-Screen (Sprache, Theme, API-Key)"
```

---

### Task 13: Web-Export + Vercel-Deploy + Ende-zu-Ende-Test

**Files:**
- Modify: ggf. `.env`-Handling (`EXPO_PUBLIC_EXPLAIN_URL`)

**Interfaces:**
- Produces: öffentliche Web-URL; funktionierender `/api/explain`-Endpoint; `EXPO_PUBLIC_EXPLAIN_URL` für native Builds.

- [ ] **Step 1: Deploy** — Skill `vercel-cli-with-tokens` verwenden (User hat Vercel-Erfahrung/Token). Projekt anlegen/verknüpfen, Env-Var `GEMINI_API_KEY` setzen (Wert vom User erfragen — Gemini-Key aus Google AI Studio), deployen.

- [ ] **Step 2: Function testen**

```bash
curl -sS -X POST https://<deployment-url>/api/explain \
  -H 'Content-Type: application/json' \
  -d '{"text":"Des Morgens sage dir selbst: Heute werde ich Zudringlichen begegnen.","reference":"Buch II, 1"}'
```

Expected: Deutscher Erklärtext streamt. Bei 429: Free-Tier-Kontingent prüfen; bei 500: Vercel-Logs lesen.

- [ ] **Step 3: Web-App im Deploy testen** — Browser: Zitate, Sprachen, „Erklären" ohne Key (Fallback über gleiche Domain `/api/explain`), mit Key (direkter Claude-Call).

- [ ] **Step 4: Native Konfiguration** — `.env` im Repo anlegen: `EXPO_PUBLIC_EXPLAIN_URL=https://<produktions-domain>/api/explain` (Expo liest `EXPO_PUBLIC_*` automatisch; `.env` committen, enthält kein Geheimnis).

- [ ] **Step 5: Commit**

```bash
git add .env && git commit -m "chore: Produktions-URL für Erklär-Endpoint"
```

---

### Task 14: Claude-Design-Projekt (Referenzkarten)

**Files:**
- Create: `design-system/palette.html`, `design-system/typography.html`, `design-system/quote-card.html`, `design-system/buttons.html`

**Interfaces:**
- Consumes: Farb-/Typo-Werte aus `theme/tokens.ts` (Werte hart einkopieren, HTML ist eigenständig).
- Produces: Design-System-Projekt „Aurelius" auf claude.ai/design mit 4 Karten.

- [ ] **Step 1: HTML-Karten bauen.** Jede Datei beginnt in Zeile 1 mit einem `@dsCard`-Marker. Muster (für alle 4 Dateien analog, jeweils mit den echten Token-Werten aus `theme/tokens.ts`):

`design-system/palette.html`:
```html
<!-- @dsCard group="Colors" -->
<!doctype html><html><head><meta charset="utf-8"><style>
  body { margin: 0; font-family: Georgia, serif; background: #F4EEE1; padding: 24px; }
  .sw { display: inline-block; width: 120px; margin: 8px; }
  .chip { height: 64px; border-radius: 12px; border: 1px solid #E2D9C6; }
  .lbl { font-size: 12px; color: #5A6575; margin-top: 6px; letter-spacing: 1px; }
</style></head><body>
  <div class="sw"><div class="chip" style="background:#F4EEE1"></div><div class="lbl">ELFENBEIN #F4EEE1</div></div>
  <div class="sw"><div class="chip" style="background:#1B2531"></div><div class="lbl">TINTE #1B2531</div></div>
  <div class="sw"><div class="chip" style="background:#A6763C"></div><div class="lbl">BRONZE #A6763C</div></div>
  <div class="sw"><div class="chip" style="background:#0F151D"></div><div class="lbl">NACHT #0F151D</div></div>
  <div class="sw"><div class="chip" style="background:#C9A264"></div><div class="lbl">BRONZE HELL #C9A264</div></div>
</body></html>
```

`typography.html` (group="Type"): Zeilen mit Cormorant Garamond (Google-Fonts-`<link>`), GFS Didot mit polytonischem Beispiel (`Ἕωθεν προλέγειν ἑαυτῷ`), UI-Sans. `quote-card.html` (group="Components"): die Zitat-Karte in Light und Dark nebeneinander, mit Stellenangabe `BUCH IV, 7` in Bronze. `buttons.html` (group="Components"): Erklären-Button, Segment-Control in beiden Themes.

- [ ] **Step 2: Lokal prüfen** — jede Datei kurz im Browser öffnen (`xdg-open design-system/palette.html`), Darstellung ok.

- [ ] **Step 3: Mit DesignSync hochladen** — Reihenfolge einhalten: `list_projects` → falls kein passendes Projekt: `create_project` mit `name: "Aurelius"` → `finalize_plan` mit `writes: ["design-system/**/*.html"]`, `localDir` = Repo-Root → `write_files` mit den 4 Dateien (per `localPath`). Danach dem User den Link zum Projekt nennen.

- [ ] **Step 4: Commit**

```bash
git add design-system && git commit -m "feat: Design-Referenzkarten für claude.ai/design"
```

---

### Task 15: Android-APK via EAS Build

**Files:**
- Create: `eas.json`

- [ ] **Step 1: `eas.json` anlegen**

```json
{
  "cli": { "appVersionSource": "remote" },
  "build": {
    "preview": {
      "android": { "buildType": "apk" },
      "env": { "EXPO_PUBLIC_EXPLAIN_URL": "https://<produktions-domain>/api/explain" }
    }
  }
}
```

(`<produktions-domain>` durch die echte Domain aus Task 13 ersetzen.)

- [ ] **Step 2: Login & Build** — braucht ein (kostenloses) Expo-Konto; den User einloggen lassen:

```bash
npx eas-cli login        # interaktiv — User macht das selbst (`! npx eas-cli login` im Prompt)
npx eas-cli build -p android --profile preview
```

Expected: Build läuft in der Expo-Cloud, am Ende Download-Link zur APK. Link dem User geben; Installation auf dem Android-Gerät (Sideload) testen: Zitate offline, Erklären mit/ohne Key.

- [ ] **Step 3: Commit**

```bash
git add eas.json && git commit -m "chore: EAS-Build-Profil für Android-APK"
```

---

### Task 16: README & Abschluss

**Files:**
- Create: `README.md`

- [ ] **Step 1: README schreiben** — Inhalt: Was ist Aurelius (2 Sätze); Screens; Entwicklung (`npm install`, `npx expo start`, Test via Expo Go/Browser); Tests (`npm test`); Daten-Pipeline (`npx tsx scripts/extract-de.ts` etc., Verweis auf `data/SOURCES.md`); Deploy (Vercel, Env-Var `GEMINI_API_KEY`); APK (`npx eas-cli build -p android --profile preview`); Hinweis auf BYOK und wo man einen Anthropic-Key bekommt (console.anthropic.com).

- [ ] **Step 2: Gesamtverifikation** — `npm test` (alle grün), `npx tsc --noEmit`, `npx expo export -p web` (baut), Erfolgskriterien der Spec Abschnitt 9 einzeln durchgehen und Ergebnis notieren.

- [ ] **Step 3: Commit**

```bash
git add README.md && git commit -m "docs: README"
```
